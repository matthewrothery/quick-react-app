import React from 'react';
import ReactDOM from 'react-dom';
import App from 'Containers'

// Render the index container under the root element
ReactDOM.render(<App />, document.getElementById('root'));